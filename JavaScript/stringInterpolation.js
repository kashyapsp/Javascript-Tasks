/* template literals means when a string is enclosed in backtics(`     `).
Example: `a and b are in the same location memory`;  */

// String Interpolation:
/*function myInfo(firstName, lastName, country) {
    return `My name is ${firstName} ${lastName}. ${country} is my favorite
    country`;
    }
    console.log(myInfo("Aa", "Bb", "India"));*/


// Function call:
function hello() {
    return "hello";
    }
 console.log(`${hello()} ! I am a student.`);


 //console.log(`Sum of two numbers ${1+1}`);